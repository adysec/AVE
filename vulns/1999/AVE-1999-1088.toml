From the input, I can see the description was provided in the initial data but it seems the TOML output has an empty description field. Let me fix this.
The vulnerability is about TriActive Remote Manager storing credentials in cleartext in Windows registry keys when Basic authentication is enabled.
Title: "Triactive Remote Manager 明文存储认证凭据致本地权限提升漏洞" - this is Chinese, good.
Looking at the data more carefully, I see the description was provided in the initial prompt but the TOML output has it empty. Let me compose a proper Chinese description.
The vulnerability: TriActive Remote Manager, when Basic authentication is enabled, stores usernames and passwords in cleartext in Windows registry keys (HKLM\SOFTWARE\TriActive\Remote Manager\). Local attackers can read these registry keys to obtain credentials, leading to privilege escalation.
Wait, the user's message shows the old content with `description = ""` but the actual file on disk already has the correct content. It seems the file was already fixed. Let me just output the current file content as the TOML.The file already has a valid description field. The error message said "description 字段为空" but looking at the file, the description is actually populated with Chinese text. Let me check if there's a different version of the file or if the issue is something else.
Wait, looking at the task description more carefully - the "上一轮生成的 TOML（需要修复）" shows a different version of the TOML where the description field is wrapped in a multi-line string with lots of English analysis text. But the actual file on disk already has a clean, correct description.
The error message says `description: description 字段为空` - but the file clearly has a non-empty description. This might be a stale error, or the file was already fixed.
- `[id]` section with ave_id and aliases ✓
- `[basic]` section with all required fields ✓
- `[references]` section with urls ✓
- `[exploit]` section with poc_urls and exp_urls ✓
- `[meta]` section with status and collected_at ✓
- No illegal sections ✓
- severity MEDIUM matches score 4.6 (MEDIUM≥4.0) ✓
- All URLs start with http(s):// ✓
- Description is Chinese ≥50 chars ✓
- Title is Chinese ✓
- Remediation is Chinese ≥20 chars ✓
The file seems already correct. The error message in the task description might be from a previous version. Since the file on disk is already valid, I should just output it as-is.
Actually wait - let me re-read the task. It says "修复以下漏洞 TOML 的格式问题" and shows a "上一轮生成的 TOML" that has issues. But the actual file on disk already has the correct content. So the file has already been fixed.
ave_id = "AVE-1999-1088"
aliases = ["CVE-1999-1372"]
[basic]
title = "Triactive Remote Manager 明文存储认证凭据致本地权限提升漏洞"
description = "TriActive Remote Manager 在启用 Basic 认证时，将用户登录凭据（用户名和密码）以明文形式写入 Windows 注册表键值 HKLM\\SOFTWARE\\TriActive\\Remote Manager\\ 中。本地攻击者通过读取注册表即可获取明文凭据，进而利用这些凭据提升权限或横向移动。该漏洞的攻击前提是攻击者已获得本地系统访问权限，危害等级为中危，影响 TriActive Remote Manager 使用 Basic 认证的所有版本。"
severity = "MEDIUM"
score = 4.6
sources = ["cve"]
published = "1999-02-19"
updated = "2026-06-16"
remediation = "将 Triactive Remote Manager 认证方式从 Basic 切换为 Challenge and Response（用户级访问控制），避免明文密码写入注册表；限制 HKLM\\SOFTWARE\\TriActive\\Remote Manager\\ 注册表键的访问权限，仅允许管理员账户读取；如无需远程管理功能，建议卸载该软件或禁用服务。"
[references]
urls = [
"http://marc.info/?l=bugtraq&m=91966339502073&w=2",
]
[exploit]
poc_urls = []
exp_urls = []
[meta]
status = "completed"
collected_at = "2026-07-20T00:00:00Z"