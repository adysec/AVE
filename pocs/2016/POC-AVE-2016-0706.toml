[info]
author = "AVE"
description = "通过 FillFromPrototypes 类型混淆漏洞触发 Chakra 引擎内存破坏，结合 CVE-2016-7200 信息泄露实现任意地址读写，最终执行 calc.exe。需在本地搭建 HTTP 服务器，受害机使用 Edge 浏览器访问该 HTML 页面即触发。"
id = "POC-AVE-2016-0706"
name = "Microsoft Edge Chakra FillFromPrototypes Type Confusion PoC"
severity = "high"
tags = [
    "cve-2016-7201",
    "edge",
    "chakra",
    "type-confusion",
]
vuln_id = [
    "CVE-2016-7201",
    "AVE-2016-0706",
]

[poc]
logic = "all"

[[poc.requests]]
method = "GET"
path = "/{{BaseURL}}/FillFromPrototypes_TypeConfusion.html"
protocol = "http"

[poc.requests.body]
content = ""
type = "raw"

[[poc.requests.matchers]]
expect = true
part = "body"
type = "word"
words = ["TriggerFillFromPrototypesBug"]
