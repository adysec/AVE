1. `remediation` field is reported as empty - looking at the TOML, the remediation field has a very long string that starts with "Let me analyze" which is not a proper remediation. The actual remediation content is there but buried in analysis text. I need to clean this up to just the actionable remediation in Chinese.
2. The `published` field has a timestamp format `1999-12-31T05:00:00.000` - this should be just a date `1999-12-31` per the schema.
3. The `updated` field has `2026-06-16T21:49:43.830` - should be just a date `2026-06-16`.
4. The `sources` field has `["epss"]` - this should be more appropriate. For a CVE, it should include "cve" at minimum.
5. The `[basic]` section should come after `[id]` per the schema order.
1. Fix remediation to be clean Chinese text without analysis preamble
2. Fix published/updated to date-only format
3. Fix sources to include "cve"
4. Reorder sections to match schema: [id], [basic], [references], [exploit], [meta]
CVE-1999-1104 is about Windows 95's weak password caching encryption (.pwl files). The remediation should be:
- Disable password caching in Windows 95
- Upgrade to a modern Windows version
1. The remediation field is the main problem - it's marked as empty by the validator. Looking at the TOML, the remediation field actually has content but it's wrapped in a multi-line string with some analysis text at the beginning. The validator might be seeing it as empty because of the way it's structured, or the content before the actual remediation text is causing issues.
2. Let me also check other issues:
- `published` should be a date, not a datetime - the schema says `published = "1999-12-31"` format
- `updated` should be a date too
- `sources` should include "cve" since this is from CVE
- The remediation has analysis text mixed in with the actual recommendation
1. Fix `published` to just date format: `"1999-12-31"`
2. Fix `updated` to just date format: `"2026-06-16"`
3. Fix `remediation` to be clean Chinese text without the analysis preamble
4. Fix `sources` to include "cve"[id]
ave_id = "AVE-1999-0944"
aliases = ["CVE-1999-1104"]
[basic]
title = "Microsoft Windows 95 密码缓存弱加密权限提升漏洞"
description = "Windows 95 在启用密码缓存功能时，对密码列表文件（.pwl）使用了弱加密算法，本地攻击者可通过解密该文件获取存储的用户密码，从而提升权限或冒充其他用户登录系统。该漏洞影响 Windows 95 全版本，攻击者须具备本地访问能力方可利用。由于 .pwl 文件中密码以可逆加密方式存储，攻击者可借助公开的解密工具（如 PWLTools）破解密码，进而获取管理员权限或访问受保护资源。"
severity = "MEDIUM"
score = 4.6
sources = ["cve", "epss"]
published = "1999-12-31"
updated = "2026-06-16"
remediation = "在 Windows 95 系统中，通过「控制面板 → 密码 → 密码属性」取消勾选「使用密码缓存」以禁用 .pwl 文件生成。根本解决方法是升级至 Windows 2000 及以上版本，这些版本采用更强的加密算法（如 RC4）保护凭据缓存。企业管理环境中应通过组策略禁用密码缓存。"
[references]
urls = [
"http://marc.info/?l=bugtraq&m=87602167418931&w=2",
"http://marc.info/?l=bugtraq&m=88536273725787&w=2",
"http://marc.info/?l=ntbugtraq&m=88540877601866&w=2",
"http://support.microsoft.com/support/kb/articles/q140/5/57.asp",
"http://www.iss.net/security_center/static/71.php",
]
[exploit]
poc_urls = []
exp_urls = []
[meta]
status = "completed"
collected_at = "2026-07-20T00:00:00Z"